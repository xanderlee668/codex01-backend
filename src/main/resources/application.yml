spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codex?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: codex
    password: codexpass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    property-naming-strategy: SNAKE_CASE

jwt:
  issuer: ${JWT_ISSUER:codex01-backend}
  expiration: ${JWT_EXPIRATION:PT2H}
  secret: ${JWT_SECRET:change-me-in-production}

server:
  port: 8080
